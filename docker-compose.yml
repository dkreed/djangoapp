version: '3'
services:
  python:
    # image: python:3.6
    build:
      context: .
      dockerfile: docker/python/Dockerfile
    volumes:
      - ./mysite:/mysite
    expose: "8000"
    ports:
      - "8000:8000"
    
   
